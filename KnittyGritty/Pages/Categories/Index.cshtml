@page
@model KnittyGritty.Pages.Categories.IndexModel

@{
    ViewData["Title"] = "Kategorier";
}

<h1>Kategorier</h1>

<p>
    <a asp-page="Create">Lägg till</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Category[0].CategoryName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Category) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CategoryName)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.CategoryID"><i class="bi bi-pencil-fill"></i></a>
                <a asp-page="./Details" asp-route-id="@item.CategoryID"><i class="bi bi-info-circle-fill"></i></a>
                <a asp-page="./Delete" asp-route-id="@item.CategoryID"><i class="bi bi-trash3-fill"></i></a>
            </td>
        </tr>
}
    </tbody>
</table>
