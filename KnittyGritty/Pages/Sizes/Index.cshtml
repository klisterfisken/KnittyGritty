@page
@model KnittyGritty.Pages.Sizes.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Size[0].SizeName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Size[0].SortOrder)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Size) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.SizeName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SortOrder)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.SizeID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.SizeID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.SizeID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
